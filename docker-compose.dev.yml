version: '3.8'

services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-fitsync_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-fitsync_password}
      POSTGRES_DB: ${DB_NAME:-fitsync_db}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fitsync_user -d fitsync_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile.dev
    restart: always
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}?schema=public
      NODE_ENV: development
    ports:
      - "3005:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/backend/node_modules
      - /app/apps/mobile/node_modules
      - /app/packages/shared/node_modules
      - /app/packages/prisma/node_modules
    depends_on:
      db:
        condition: service_healthy
    command: >
      sh -c "npm install &&
             npm run generate --workspace=@fitsync/prisma &&
             npm run db:push --workspace=@fitsync/prisma &&
             npm run dev --workspace=backend -- --webpack"

volumes:
  postgres_data:
